const PREFIX = "/api"

export const urls = {
    pageSize: 5,
    GET_DOCUMENT_ALL_URL: `${PREFIX}/documents/all`,
    GET_USERS_URL: `${PREFIX}/users`,
    // add more configuration values as needed
  };